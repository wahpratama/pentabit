import mongoose from "mongoose";
import Project from "../../utils/Models/porject"
import Connect from "../../utils/middleware/connect";

export default handler = async(req, res, next) => {
    if(req.method === "POST"){
        const Name = req.body
        if(Name){
            
        }
    }
}